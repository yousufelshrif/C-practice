#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *intigers;
    char *charachtars;
    intigers = calloc(10,sizeof(int));
    charachtars = calloc(100,sizeof(char));
    // store i variable in intgers[1]
    //store n variable in intgers[2]
    scanf("%d",&intigers[2]);

    return 0;
}
